# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# SMTP_HOST: The hostname of your real SMTP server (e.g., smtp.gmail.com)
SMTP_HOST="smtp.gmail.com"

# SMTP_PORT: The port of your real SMTP server (e.g., 587 or 465)
SMTP_PORT="587"

# SMTP_USER: Your SMTP username/email address (e.g., alert@ostvendas.com)
SMTP_USER=""

# SMTP_PASS: Your SMTP password or App Password (e.g., your-app-password)
SMTP_PASS=""

# SMTP_SECURE: True if using SSL on port 465, false if using STARTTLS on port 587
SMTP_SECURE="false"

# Google Cloud SQL (PostgreSQL) Database Credentials
DATABASE_URL=""
SQL_HOST=""
SQL_PORT="5432"
SQL_USER=""
SQL_PASSWORD=""
SQL_DB_NAME="ostvendas"
SQL_ADMIN_USER="postgres"
SQL_ADMIN_PASSWORD=""

# Supabase (PostgreSQL) Database & Auth Integration
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
